#include <stdio.h>
int main()
{
    char a[10];
    char b[10];
    char combine[50];
    scanf("%s%s",&a,%b);
    sprintf(combine,"%s and %s",a,b);
    printf("You enterd: %s\n":combine);

    return 0;
}